let month = prompt('Введите число от 1 до 12');
    if (month <= 3) {
        alert("Зима");
    }
    else if (month <= 6) {
        alert("Весна");
    }
    else if (month <= 9) {
        alert("Лето");
    }
    else if (month <= 12) {
        alert("Осень");
    }

